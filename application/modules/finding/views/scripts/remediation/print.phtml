<?php
    $parts = array(
        'finding',
        'risk-analysis',
        'security-control',
        'comments',
        'artifacts',
        'audit-log'
    );
    foreach ($parts as $part) {
        echo $this->partial('remediation/' . $part . '.ajax.phtml', $this->getVars());
    }
