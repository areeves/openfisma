UserOrganization:
    listeners: [CacheListener]

    columns:
        userId:
            type: integer
            primary: true
        organizationId:
            type: integer
            primary: true
