DocumentType:
    listeners: [XssListener, CacheListener]
    
    columns:
        name: 
            type: string(255)
            extra:
                logicalName: Document Name
                template: true
            comment: A human-readable description of the file type, e.g. System Security Plan
